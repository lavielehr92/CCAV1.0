"""
`scripts.utils` package initializer.

This makes `from scripts.utils.data_quality import ...` usable in deployments where
the `scripts` directory must be a Python package.
"""

__all__ = ["data_quality"]